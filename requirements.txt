# Python dependencies for Cursor Agent System

# Core dependencies
python-dotenv>=1.0.0

# Dashboard dependencies (optional, only needed for --dashboard mode)
rich>=13.0.0
textual>=0.40.0

# Intent tracking dependencies
pyyaml>=6.0

# Web dashboard (optional, run independently of main.py)
fastapi>=0.100.0
uvicorn[standard]>=0.22.0
jinja2>=3.1.0
